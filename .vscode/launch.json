{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "CUDA C++: Launch",
      "type": "cuda-gdb",
      "request": "launch",
      "program": "${workspaceFolder}/train_gpt2fp32cu",
      "preLaunchTask": "nvcc",
      "cwd": "${workspaceFolder}",
    }
  ]
}